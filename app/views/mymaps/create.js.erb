$(".modal-title").html("Show MyMap");
$(".modal-body").html("<%= j(render "show") %>");

// Change MyMap button to show saved MyMap.
$("#new_mymap_<%= @restaurant.id %>").replaceWith("<%= j(mymap_button(@user, @restaurant)) %>");
