$("#new_comment").bind("ajax:success", function() {
	$("#comment_count").html("<%= @restaurant.comments.count %>");
	$("#comment_section").html("<%= j(render partial: "comments/show", collection: @restaurant.comments, as: :comment) %>");
});