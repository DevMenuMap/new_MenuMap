<li id="comment_<%= comment.id %>">
	<div style="width: 340px;"><%= comment.contents %></div>
	<% if star_rating_exists?(comment) %>
		<div class="star_rating" data-score=<%= comment.rating%>></div>
	<% end %>
</li>

<%#= time_ago_in_words(comment.created_at) %>
<%#= link_to "edit", edit_comment_path(comment), class: "edit_comment", remote: true %>
<%#= link_to "delete", comment_path(comment), class: "delete_comment", method: :delete, remote: true, data: { confirm: "gogo?" } %>
