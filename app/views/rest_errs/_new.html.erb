<%= form_for [@restaurant, @rest_err], html: { mutipart: true } do |f| %>
	<%= f.hidden_field :restaurant_id, value: @restaurant.id %>

	<%= render "rest_errs/field", f: f %>
	<div class="modal-footer" style="padding: 5px;">
		<%= f.button "완료", class: "btn btn-primary" %>
	</div>
<% end %>
